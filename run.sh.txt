#!/bin/bash
# Executa o motor uma vez para garantir que os dados existem
python motor.py
# Inicia o servidor Streamlit em seguida
streamlit run app.py --server.port=10000 --server.address=0.0.0.00.0.0