#!/bin/bash
# Executa o motor em segundo plano (&) para não prender o processo do Streamlit
python motor.py &

# O comando que mantém o serviço "vivo" deve ser o último
# O --server.port=$PORT usa a variável do Render automaticamente
streamlit run app.py --server.port=$PORT --server.address=0.0.0.0
